# Contributing

Use .NET 8, keep nullable analysis and warnings-as-errors enabled, add tests for behavior changes, and never include credentials or unredacted recordings. Security-sensitive reports should not include live secrets or private screenshots.

Pull requests must pass the Windows CI build and tests. Native desktop changes should include the tested Windows build, DPI, monitor layout, target application, integrity level, and session type.
