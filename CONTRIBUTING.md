# Contributing

Use .NET 8, keep nullable analysis and warnings-as-errors enabled, and add tests for behavior changes. Never commit credentials or unredacted recordings.

Native desktop changes should document the Windows build, DPI, monitor layout, target application, integrity level, and session type used for testing.
