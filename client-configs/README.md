# Client configuration

Run `install/register-clients.ps1` after installation. It produces Codex, Claude Code, and Antigravity MCP snippets containing the correct local executable and data paths. Client configuration locations may change; consult each client's current documentation before replacing existing configuration.
